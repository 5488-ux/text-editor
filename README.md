# text-editor (iOS)

SwiftUI iOS 文本编辑器（带导入/导出、三栏底部导航、后台信息页）。

## 开发

本项目使用 XcodeGen 生成工程：

```bash
brew install xcodegen
xcodegen generate
open TextEditor.xcodeproj
```

## CI

仓库包含一个 GitHub Actions 的 iOS 构建工作流（模拟器构建）。

